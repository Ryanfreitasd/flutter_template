name: Setup Codespaces for Flutter

on:
  codespace:

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Configure Codespaces for Flutter
        run: |
          echo "🚀 Codespaces Flutter environment ready!"
          echo "📱 Navigate to: https://$CODESPACE_NAME-8080.app.github.dev"
          echo "🔧 DevTools: https://$CODESPACE_NAME-9100.app.github.dev"
